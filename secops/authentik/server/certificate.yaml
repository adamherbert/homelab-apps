apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-outpost-tls
  namespace: authentik
spec:
  secretName: authentik-outpost-tls
  issuerRef:
    name: internal
    kind: ClusterIssuer
  dnsNames:
    - outpost.lab.8none1.com
