apiVersion: v1
kind: Service

metadata:
  name: authentik-outpost
  namespace: media
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 9000
  externalName: authentik-outpost.authentik.svc.cluster.local
